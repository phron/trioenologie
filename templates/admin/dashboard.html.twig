{% extends 'base.html.twig' %}

{% block title %}Dashboard de {{ app.user.email }} !{% endblock %}

{% block body %}

<div class="container">


    <h1 class="mt-3 text-center">Administration du site</h1>

        <div class="row justify-content-center col-lg-12 col-md-8 col-sm-4 mt-3">
        
            <a href="{{ path('admin_article_index') }}" class="col-4 text-center btn btns btn-article">Articles</a>
            <a href="{{ path('admin_occasion_index') }}" class="col-4 text-center btn btns btn-evenements">Evènements</a>
            <a href="{{ path('admin_gallery_index') }}" class="col-4 text-center btn btns btn-galerie">Galerie photo</a>

        </div>

        <div class="row justify-content-center col-lg-12 col-md-8 col-sm-4">
            <a href="{{ path('admin_user_index') }}" class="col-4 text-center btn btns btn-users">Utilisateurs</a>
            <a href="{{ path('admin_categories') }}" class="col-4 text-center btn btns btn-categories">Catégories</a>
            <a href="{{ path('admin_carousel') }}" class="col-4 text-center btn btns btn-carousel">Carousel</a>
        </div>        

</div>

{% endblock %}
