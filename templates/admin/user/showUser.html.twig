{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
<div class="container col-lg-12 col-md-10 col-sm-8 mt-5">

    <h1 class="text-center rF mt-3 mb-3">Utilisateur : {{user.email}}</h1>

    <table class="table text-center table-borderless table-striped">
        <tbody class="tab">
            <tr>
                <th class="col-2 rFi">ID</th>
                <td class="rF">{{ user.id }}</td>
            </tr>
            <tr>
                <th class="col-2 rFi">E-mail</th>
                <td class="rF">{{ user.email }}</td>
            </tr>
            <tr>
                <th class="col-2 rFi">Rôle</th>
                <td class="rF">{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>

    <div class="row justify-content-center">
        <div class="col-3 ms-5">
            <a href="{{ path('admin_user_index') }}" class="btn btn-return">
                <i class="bi bi-arrow-left-circle-fill"></i>
            </a>      
            <a href="{{ path('admin_user_edit', {'id':user.id}) }}" class="btn btn-update">
                <i class="bi bi-pencil"></i>
            </a>
        </div>
    </div>

</div>
{% endblock %}
