{% extends 'base.html.twig' %}

{% block title %}Status{% endblock %}

{% block body %}
    <h1>Status</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ status.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ status.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_status_index') }}">back to list</a>

    <a href="{{ path('admin_status_edit', {'id': status.id}) }}">edit</a>

    {{ include('admin/status/deleteStatus.html.twig') }}
{% endblock %}
