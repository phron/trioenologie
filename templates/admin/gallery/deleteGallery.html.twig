<form method="post" action="{{ path('admin_gallery_delete', {'id': gallery.id}) }}" onsubmit="return confirm('Êtes-vous sûr.e de vouloir supprimer cette image de la galerie?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gallery.id) }}">
    <button class="btn btn-delete">Supprimer l'image de la galerie</button>
</form>
